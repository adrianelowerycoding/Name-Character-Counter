# Name-Character-Counter
A simple EJS project that counts the number of characters is a submitted name and returns the value to the user. Helping me learn form submission, POST requests, and passing data both from server to EJS and EJS to server. 
